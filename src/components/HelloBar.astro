---
interface Props {
    enabled?: boolean;
    text?: string;
    linkText?: string;
    linkUrl?: string;
    storageKey?: string;
    cooldownDays?: number;
}

const {
    enabled = true,
    text = "ClaudeKit Marketing v1.3.0 just dropped",
    linkText = "See what's new",
    linkUrl = "https://claudekit.cc/?ref=BIZE9CYY",
    storageKey = "hellobar-claudekit-v130",
    cooldownDays = 3
} = Astro.props;
---

{enabled && (
    <div class="hello-bar" id="hello-bar" data-storage-key={storageKey} data-cooldown-days={cooldownDays}>
        <div class="hello-bar-content">
            <span class="hello-bar-icon">&#x1F4E6;</span>
            <span class="hello-bar-text">{text}</span>
            <a href={linkUrl} target="_blank" rel="noopener noreferrer" class="hello-bar-link">
                {linkText} &rarr;
            </a>
        </div>
        <button class="hello-bar-close" aria-label="Close banner">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M18 6L6 18M6 6l12 12"/>
            </svg>
        </button>
    </div>
)}

<style>
    .hello-bar {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        background: linear-gradient(90deg, #1a1a1a 0%, #2d1f3d 100%);
        color: white;
        padding: 10px 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 16px;
        z-index: 1000;
        font-size: 0.9em;
        transform: translateY(-100%);
        opacity: 0;
        transition: transform 0.3s ease, opacity 0.3s ease;
    }

    .hello-bar.visible {
        transform: translateY(0);
        opacity: 1;
    }

    .hello-bar.hiding {
        transform: translateY(-100%);
        opacity: 0;
    }

    .hello-bar-content {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .hello-bar-icon {
        font-size: 1.1em;
    }

    .hello-bar-text {
        color: rgba(255, 255, 255, 0.9);
    }

    .hello-bar-link {
        color: #00D4AA;
        font-weight: 600;
        text-decoration: none;
        transition: color 0.15s;
    }

    .hello-bar-link:hover {
        color: #00F5C4;
        text-decoration: underline;
    }

    .hello-bar-close {
        position: absolute;
        right: 16px;
        background: none;
        border: none;
        color: rgba(255, 255, 255, 0.5);
        cursor: pointer;
        padding: 4px;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: color 0.15s;
    }

    .hello-bar-close:hover {
        color: white;
    }

    @media (max-width: 600px) {
        .hello-bar {
            padding: 10px 40px 10px 16px;
            font-size: 0.82em;
        }

        .hello-bar-content {
            flex-wrap: wrap;
            justify-content: center;
            gap: 4px 8px;
        }
    }
</style>

<script>
    const bar = document.getElementById('hello-bar');
    if (bar) {
        const storageKey = bar.dataset.storageKey || 'hellobar-dismissed';
        const cooldownDays = parseInt(bar.dataset.cooldownDays || '3', 10);
        const dismissedAt = localStorage.getItem(storageKey);

        let shouldShow = true;
        if (dismissedAt) {
            const elapsed = Date.now() - parseInt(dismissedAt, 10);
            const cooldownMs = cooldownDays * 24 * 60 * 60 * 1000;
            if (elapsed < cooldownMs) {
                shouldShow = false;
            } else {
                localStorage.removeItem(storageKey);
            }
        }

        if (shouldShow) {
            setTimeout(() => bar.classList.add('visible'), 100);

            const closeBtn = bar.querySelector('.hello-bar-close');
            closeBtn?.addEventListener('click', () => {
                bar.classList.add('hiding');
                bar.classList.remove('visible');
                localStorage.setItem(storageKey, String(Date.now()));
                setTimeout(() => bar.remove(), 300);
            });
        } else {
            bar.remove();
        }
    }
</script>
