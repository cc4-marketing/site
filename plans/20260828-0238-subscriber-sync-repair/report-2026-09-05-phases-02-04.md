# Report: phases 02-04 execution, 2026-09-05

Everything buildable without Substack dashboard access is built and tested. Dashboard steps are runbook-ready below.

## Done (code, local, uncommitted)

Phase 03 (complete except live acceptance run):
- scripts/lib/subscriber-sync-diff.mjs: pure four-bucket diff, tolerant Substack CSV parser, normalization at every boundary
- scripts/check-subscriber-sync.mjs: CLI. Buckets 1/2 fail loud (exit 1), bucket 3 count-only, bucket 4 listed. `--apply` suppresses bucket 2 on Resend (suppression only, never resubscribes, never writes Substack). `--emit-import-csv <path>` writes bucket 1 as the phase 02 import file
- scripts/__tests__/subscriber-sync-diff.test.mjs: fixtures cover all four buckets + case/whitespace variants + CSV quoting
- package.json: `check:subscriber-sync` script; vitest include extended to scripts/**/__tests__
- Deviation from plan: the original /tmp 2026-08-28 exports no longer exist, so the "must reproduce 130/2" acceptance run needs fresh exports; first real run doubles as it (expect bucket 1 ≈ 130 + new signups, bucket 2 = 2, before phase 02 lands)

Phase 04 step 2 (email split):
- Resend download email now owns only download + quick start. Subject changed to "Your Claude Code for Marketers download link". Newsletter identity, cadence, archive belong to the Substack welcome only

Phase 04 step 3 (cadence decision):
- Weekly, Tuesday 9:00 AM ET (8:00 PM VN). Promised in the welcome email text below. Hold three months before judging open/click numbers

## Substack welcome email (phase 04 step 1, paste into Settings → Welcome email)

Subject suggestion: Welcome, here is how this works

---
Hey Vibe Marketer,

You just joined the newsletter for Claude Code for Marketers, the free interactive course at cc4.marketing.

One thing to do first: download the course and type /start-0-0 in Claude Code. The whole course runs inside it. Get it at cc4.marketing/download.

What to expect from this list: one issue every Tuesday. Real marketing workflows from a real site, with the receipts attached: what we shipped, what broke, what it cost, and the exact commands behind it.

Catch up anytime in the archive: cc4marketing.substack.com/archive

Got a build or a question? Reply to any issue. I read every message.
---

## Runbook: remaining dashboard steps (need Substack login)

Order matters. Phase 01 deploy + verify comes first (its own consent), then:

1. Phase 01 step 1: Settings → check double opt-in for free subscribers. If ON, either turn off (site form is explicit opt-in) or change download.astro success copy to "check your inbox to confirm". Step 2: check support.substack.com for an official write API; if one exists, swap the endpoint in subscribe.ts.
2. Phase 02 step 5 (do immediately, no tooling needed): Subscribers → remove hanpris@gmail.com and chanderr1995@outlook.com. They opted out in April.
3. Phase 04 step 1: Settings → Welcome email → paste copy above, enable.
4. Fresh exports: Substack Settings → Subscribers → Export. Then run:
   `RESEND_API_KEY=$(op read "op://...") RESEND_AUDIENCE_ID=$(op read "op://...") npm run check:subscriber-sync -- --substack <export.csv> --emit-import-csv /tmp/substack-import.csv`
   Expect bucket 1 ≈ 130+, bucket 2 = 2 (0 after step 2 above). This is the phase 03 acceptance run.
5. Phase 02 step 2: Subscribers → Import → upload 5 addresses from /tmp/substack-import.csv first, confirm they land as free subscribers on CC4.Marketing, then import the rest. Import AFTER the welcome email is live (step 3 choice: preferred path from the plan, stranded people get the welcome as their first touch).
6. Re-run the sync check: buckets 1 and 2 must read 0, exit 0.
7. Weekly habit: export + `npm run check:subscriber-sync` alongside each Tuesday send. Drop to monthly after two clean months.

## Phase 02 step 3 decision (recorded)

Preferred path taken: import waits for the welcome email. Stranded subscribers get the same first touch as everyone else instead of a cold broadcast or an apology line.

## Not done, by design

- Phase 04 step 4 (click problem): flagged, revisit after 3 months of steady cadence.
- Phase 04 step 5: the 94 dead addresses stay; re-measure after three real sends.
- CI wiring for the sync check: intentionally manual (needs a human-fetched export).

## Execution log, 2026-09-05 (all runbook steps done)

- Phase 01 deployed (commit 91be53e, CI deploy success). Live probe: `a@b` now 400.
- Double opt-in: OFF. Phase 01 is a full fix, success copy unchanged.
- Official API check: Substack offers only a read-only MCP server (Bestseller pubs). No subscriber writes. Unofficial endpoint stays.
- The two April opt-outs removed from Substack (detail pages 404).
- Both welcome emails edited and saved (existing good copy kept): greeting "Hey Vibe Marketer,", cadence promise "One issue every Tuesday", lesson count fixed 17/3 → 19/4; imported variant additionally names the sync gap honestly.
- Fresh export 3:11 AM (676 rows) + Resend API (444 contacts, 11 unsub) through check:subscriber-sync:
  bucket 1 = 144 (audit's 130 + pre-deploy leakage), bucket 2 = 0 (removals verified), bucket 3 = 387 (exact audit match), bucket 4 = 0. Script reproduces the audit: phase 03 acceptance passed.
- Import: staged. Trial 5 → 4 added + 1 dedupe (adam@nerdsdoitbetter.com already existed since Apr, email-disabled; import did NOT re-enable his emails, confirming compliance safety). Then 135 via CSV upload → Substack reports Imported 116/116, 0 failed (19 deduped). Spot checks across the batch: Active / Free / Sep 5.
- Excluded from import as justifiable residue (4): aswinwrites@gmial.com (typo domain), josephlekha95@gmail.com.com, rafay.siddiqui58@gmail.comcom (correct twin imported), mtri.vo+test@gmail.com (owner test).
- Substack subscriber count cache still shows 677; expected ~797 after refresh.

## Still open

- Second same-day export appears rate-limited, so the buckets-to-zero re-run happens at the first weekly cadence run: export → `npm run check:subscriber-sync -- --substack <csv>`. Expect bucket 1 ≈ 4 (the excluded residue) and bucket 2 = 0.
- End-to-end signup test with a fresh user-controlled address in Safari with popups blocked (phase 01 step 8 acceptance 4).
- GitHub Actions secret CLAUDE_CODE_OAUTH_TOKEN is empty; auto-changelog workflow failing since before this work.
- Local PII temp files under /tmp (substack export + import batches) cleared on reboot; delete earlier if desired.
